# PokemonRL — Ray RLlib experiment dependencies (unpinned)
#
# For the experimental Ray-based training in baseline/ray_exp/.
# Requires Ray nightly or a compatible release.
#
# Install: pip install -r requirements/requirements-ray.txt

ray[default,rllib,tune,air]
einops
gputil
hnswlib
numpy
seaborn
pyboy
tensorboard
mediapy
